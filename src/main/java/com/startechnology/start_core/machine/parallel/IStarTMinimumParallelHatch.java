package com.startechnology.start_core.machine.parallel;

public interface IStarTMinimumParallelHatch {
    /* Return the minimum amount of parallels allowed before this can run */
    Integer getMinimumParallels();
}
